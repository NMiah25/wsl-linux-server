# WSL Linux Server

Local Linux server environment built using Ubuntu on WSL2.

## Features

- Ubuntu Linux
- systemd
- OpenSSH Server
- Remote SSH access from Windows PowerShell
- Linux command-line administration
- Reads out stats of the server

## Setup

Installed OpenSSH Server:

```bash
sudo apt update
sudo apt install openssh-server